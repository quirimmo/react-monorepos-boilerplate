import './commands';

// after((): void => {
//   if (Cypress.env('is-recording')) {
//     cy.task('log', 'Stopping recording mode...');
//     /* eslint-disable-next-line cypress/no-unnecessary-waiting */
//     cy.wait(2000);
//   }
// });
